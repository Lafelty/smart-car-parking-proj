## Installation

To install and run this project, follow these steps:

### Steps

1.  **Install needed pip**
    ```bash
    pip install flask
    pip install opencv-python
    pip install cvzone
    pip install numpy
2. 
    
